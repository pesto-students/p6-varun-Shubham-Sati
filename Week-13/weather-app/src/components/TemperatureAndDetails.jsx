import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  padding: 0 1rem;
`;

const TemperatureAndDetails = () => {
  return <Wrapper>TemperatureAndDetails</Wrapper>;
};

export default TemperatureAndDetails;
